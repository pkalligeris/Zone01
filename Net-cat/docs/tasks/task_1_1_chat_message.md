# Task 1.1: Define ChatMessage struct and its formatting

## Description
Define the core `ChatMessage` structure and the formatting logic required by the system.

## Acceptance Criteria
- [x] Test that regular messages format correctly: `[YYYY-MM-DD HH:MM:SS][client.name]:[client.message]`.
- [x] Test that event messages format correctly: `[client.name] has joined our chat...`
- [x] Implement `ChatMessage` struct.
- [x] Implement `FormatMessage()`.
